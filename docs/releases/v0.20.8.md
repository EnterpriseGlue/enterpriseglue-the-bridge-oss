---
doc_class: technical
audience: [operator, developer, maintainer]
publication: github
lifecycle: release
---

<!-- Generated by scripts/release-notes.mjs. Update source fragments instead of editing this file. -->

# EnterpriseGlue v0.20.8 Release Notes

Generated from release-note fragments changed since `v0.20.7`.

## Highlights

- **ci-release:** Qualify each exact Release Please merge candidate once instead of rebuilding the same release payload in both CI and candidate staging.

## Database migrations

- Database rollback supported for the combined release: yes.

## Security

- The required candidate status remains bound to the exact merge-queue SHA and still requires the five-database, Operaton, package, chart, image, scan, signature, and receipt gates.
- Pipeline implementation changes continue to run the read-only release-readiness rehearsal before merge.

## Documentation

- The maintainer CI and release routing documentation explains the single authoritative heavy candidate gate.

## Known limitations

- This change removes duplicate work; it does not reduce the scope of the authoritative Release Candidate Stage.

## Rollback

- Revert the CI condition and aggregate selection expression to restore readiness qualification on Release Please heads and merge groups.

## Validation evidence

- Run release-readiness and candidate-workflow contract tests, change-classifier fixtures, aggregate coverage checks, and release-note preflight.
- Verify a release-only generated delta skips the duplicate readiness job while the merge-queue candidate still requires signed staging.

## Release metadata

- Release-note fragments: `release-staging-fast-path`
- Audiences: operators, developers, security
- Compatibility: backward-compatible
