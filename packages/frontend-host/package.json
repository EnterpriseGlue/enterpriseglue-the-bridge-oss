{
  "name": "@enterpriseglue/frontend-host",
  "version": "0.4.0",
  "description": "OSS frontend host for EnterpriseGlue — routes, enterprise loader, extension registry, and feature components",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/nicholasgriffintn/enterpriseglue-the-bridge-oss.git",
    "directory": "packages/frontend-host"
  },
  "type": "module",
  "scripts": {
    "build:shared": "npm run --prefix ../shared build",
    "typecheck": "npm run build:shared && tsc -p tsconfig.build.json --noEmit",
    "build": "npm run build:shared && tsc -p tsconfig.build.json && node scripts/copy-assets.mjs",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.js.map",
    "dist/**/*.d.ts",
    "dist/**/*.d.ts.map",
    "dist/**/*.css",
    "dist/**/*.svg",
    "dist/**/*.png",
    "src/**/*.css",
    "src/**/*.svg",
    "src/**/*.png",
    "src/**/*.d.ts",
    "proxy-routes.json",
    "assets/**/*"
  ],
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    },
    "./proxy-routes.json": "./proxy-routes.json",
    "./styles/*": "./dist/styles/*",
    "./assets/*": "./assets/*"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "dependencies": {
    "@enterpriseglue/enterprise-plugin-api": "*",
    "@enterpriseglue/shared": "*",
    "jspdf": "^4.2.1",
    "svg2pdf.js": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
