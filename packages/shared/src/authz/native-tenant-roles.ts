/** Stable system-role identifiers used by the native tenant authority. */
export const NATIVE_TENANT_ROLE_IDS = {
  ADMIN: 'system.tenant.admin',
  ENGINE_OPERATOR: 'system.tenant.engine_operator',
  VIEWER: 'system.tenant.viewer',
} as const;
