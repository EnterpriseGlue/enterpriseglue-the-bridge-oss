/** Compatibility entry point for the canonical permission-backed middleware. */
export {
  requirePlatformAdmin,
  requirePlatformRole,
  checkPlatformAdmin,
  isPlatformAdmin,
} from '../../middleware/platformAuth.js';
