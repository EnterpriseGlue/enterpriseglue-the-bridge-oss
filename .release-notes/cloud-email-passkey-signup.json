{
  "schemaVersion": 1,
  "id": "cloud-email-passkey-signup",
  "type": "feature",
  "scope": "managed Cloud account authentication",
  "breaking": false,
  "audiences": ["users", "operators", "security", "developers"],
  "summary": "Allow a new managed Cloud user to verify an email address and register a passkey without granting organization access from the address alone.",
  "userImpact": ["New Cloud users can choose an email and passkey sign-up path separate from federated sign-in."],
  "upgrade": { "required": true, "instructions": ["Apply the Cloud email-signup and passkey credential migration before enabling the new flow."] },
  "configuration": ["Managed Cloud account identity and a working global outbound email configuration are required; the relying-party origin is the configured frontend origin."],
  "api": { "compatibility": "additive", "changes": ["Add Cloud email-signup verification and passkey registration and authentication endpoints."] },
  "database": { "migrations": ["1700000000133-add-cloud-email-passkeys"], "rollbackSupported": false, "notes": ["The new pending-signup and passkey tables have no tenant membership side effects. The down migration refuses to remove populated passkey credentials."] },
  "security": ["Email verification establishes address control, while a verified passkey assertion authenticates the account. Matching email addresses never merge existing accounts or grant tenant membership."],
  "documentation": ["docs/how-to/auth-sso.md"],
  "knownLimitations": ["Lost-passkey recovery requires a separate verified support process until a recovery method is deliberately introduced."],
  "rollback": ["Disable the new Cloud email option and return users to existing federated methods before removing passkey tables; do not remove credentials while passkey-only accounts need access."],
  "validation": ["Focused signup, passkey, Cloud-session, authorization, PostgreSQL, and browser gates."],
  "packages": []
}
